<UserControl x:Class="ClientLourd.Views.Controls.Game.GameStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ClientLourd.Views.Controls.Game"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="5">
        <materialDesign:Card>
            <StackPanel>
                <ListView AlternationCount="{Binding Guess.Length}" x:Name="List" ItemsSource="{Binding Players}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding ElementName=List, Path=(ListView.AlternationIndex)}"
                                         FontSize="24" Margin="5"/>
                                    <materialDesign:Chip materialDesign:ShadowAssist.ShadowDepth="Depth1" Content="{Binding Username}">
                                        <materialDesign:Chip.Style>
                                            <Style TargetType="materialDesign:Chip">
                                                <Setter Property="Background" Value="WhiteSmoke" />
                                            </Style>
                                        </materialDesign:Chip.Style>
                                        <materialDesign:Chip.Icon>
                                            <Image Source="{Binding Avatar, UpdateSourceTrigger=PropertyChanged}"/>                                            
                                        </materialDesign:Chip.Icon>
                                    </materialDesign:Chip>
                                    <materialDesign:PackIcon Kind="Check" VerticalAlignment="Center" Foreground="Green"></materialDesign:PackIcon>
                                </StackPanel>
                                <TextBlock HorizontalAlignment="Center" Text="score: 450"></TextBlock>
                                <Separator/>
                            </StackPanel>
                        </DataTemplate>     
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </materialDesign:Card>
        
    </Grid>
</UserControl>
